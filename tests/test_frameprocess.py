import pytest
#from unittest.mock import MagicMock, patch
#from receiver_main import process_frames
#import cv2

# placeholder for until tests are required
def test_placeholder():
    assert True

# with open("data/sample_frame.txt", "r") as f:
#     sample_frame = f.read()
#     sample_frame = bytearray(sample_frame)

# @pytest.mark.parametrize("frame_payload", [sample_frame])
# def test_process_frames_valid_input(frame_payload):
#     with patch('cv2.imshow', MagicMock()):
#         process_frames(frame_payload)
#         cv2.imshow.assert_called_once_with("Frame from your_client_id2", MagicMock())